﻿@model IEnumerable<Day2_Demo_Razor.Models.Students>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<h1>Welcome on this page: @DateTime.Now</h1>
<br />

@{
    var dt = DateTime.Now;
    var msg = "This is Students List";
    if (DateTime.IsLeapYear(dt.Year))
    {
        <h1>Its a leap year</h1>
    }
    else
    {
        <h1>Not a leap year</h1>

    }

    @*<text>Todays date is:</text> @dt
        <h1>msg</h1>*@

}
<br />
<h1>@dt</h1>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RollNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RollNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>